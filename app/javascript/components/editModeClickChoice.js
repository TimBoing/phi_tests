const editModeClickChoice = () => {
  // const editMode = document.querySelector(".simple_form.edit_situation");
  // if(!editMode) return;
  // const choices = document.querySelectorAll(".choice");
  // choices.forEach((choice) => {
  //   choice.addEventListener("click", (event) => {

  //     //Si on est pas en mode edit sur le choix et qu´on est pas en mode
  //     console.log(event.target.classList);
  //   });
  // });

}

export{editModeClickChoice};
